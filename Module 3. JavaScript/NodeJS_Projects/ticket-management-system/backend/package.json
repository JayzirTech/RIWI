{
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  },
  "scripts": {
    "api:productos": "json-server --watch api/dbTickets.json --port 3000",
    "api:usuarios": "json-server --watch api/dbUsers.json --port 3001",
    "apis": "concurrently \"npm run api:usuarios\" \"npm run api:productos\""
  }
}